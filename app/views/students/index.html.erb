<h1>Student List</h1>

<%= form_tag(students_path, method: :get) do %>
  <%= text_field_tag :term %>
  <%= submit_tag 'Search' %>
<% end %>


<% @search.all.each do |student| %>
<%= render partial: "student", locals: {student: student} %>
<% end %>
